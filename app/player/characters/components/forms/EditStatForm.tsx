import { useCharacter } from "../../../api/CharacterContext"

interface EditStatFormProps {

}

export default function EditStatForm({} : EditStatFormProps) {
    return (
        <form>
            <div>Form</div>
        </form>
    )
}